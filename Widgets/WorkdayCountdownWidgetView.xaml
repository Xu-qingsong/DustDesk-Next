<UserControl x:Class="DustDesk.Next.Widgets.WorkdayCountdownWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="472"
             MinHeight="174">
    <UserControl.Resources>
        <Style x:Key="MetricLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>
        <Style x:Key="MetricValueStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Cascadia Mono, Consolas" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Margin="4,0,2,13">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="92" />
            </Grid.ColumnDefinitions>
            <StackPanel VerticalAlignment="Center">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding StatusText}" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextMutedBrush}" />
                    <TextBlock Grid.Column="1" Text="{Binding ScheduleText}" Style="{StaticResource CaptionStyle}" FontFamily="Cascadia Mono, Consolas" />
                </Grid>
                <TextBlock Text="{Binding CountdownText}"
                           Margin="-2,0,0,5"
                           FontFamily="Bahnschrift SemiCondensed, Segoe UI Variable Display"
                           FontSize="44"
                           FontWeight="SemiBold"
                           TextWrapping="NoWrap" />
                <ProgressBar Value="{Binding WorkdayProgressPercent}" Maximum="100" Height="5" />
            </StackPanel>
            <Grid Grid.Column="1">
                <TextBlock Text="&#xE821;"
                           FontFamily="Segoe Fluent Icons"
                           FontSize="54"
                           Foreground="#E5A53F"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center" />
                <TextBlock Text="&#xE823;"
                           FontFamily="Segoe Fluent Icons"
                           FontSize="22"
                           Foreground="{StaticResource PrimaryBrush}"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Bottom"
                           Margin="0,0,8,10" />
            </Grid>
        </Grid>

        <UniformGrid Grid.Row="1" Columns="{Binding MetricColumnCount}">
            <Border Background="{StaticResource SurfaceMutedBrush}" CornerRadius="4" Padding="7,7" Margin="0,0,6,0">
                <StackPanel><TextBlock Text="发薪" Style="{StaticResource MetricLabelStyle}" /><StackPanel Orientation="Horizontal" HorizontalAlignment="Center"><TextBlock Text="{Binding PaydayDaysText}" Style="{StaticResource MetricValueStyle}" /><TextBlock Text="天" Style="{StaticResource MetricLabelStyle}" Margin="3,5,0,0" /></StackPanel></StackPanel>
            </Border>
            <Border Background="{StaticResource SurfaceMutedBrush}" CornerRadius="4" Padding="7,7" Margin="0,0,6,0">
                <StackPanel><TextBlock Text="周末" Style="{StaticResource MetricLabelStyle}" /><StackPanel Orientation="Horizontal" HorizontalAlignment="Center"><TextBlock Text="{Binding WeekendDaysText}" Style="{StaticResource MetricValueStyle}" /><TextBlock Text="天" Style="{StaticResource MetricLabelStyle}" Margin="3,5,0,0" /></StackPanel></StackPanel>
            </Border>
            <Border Background="{StaticResource SurfaceMutedBrush}" CornerRadius="4" Padding="7,7" Margin="0,0,6,0">
                <StackPanel><TextBlock Text="{Binding FestivalLabel}" Style="{StaticResource MetricLabelStyle}" /><StackPanel Orientation="Horizontal" HorizontalAlignment="Center"><TextBlock Text="{Binding FestivalDaysText}" Style="{StaticResource MetricValueStyle}" /><TextBlock Text="天" Style="{StaticResource MetricLabelStyle}" Margin="3,5,0,0" /></StackPanel></StackPanel>
            </Border>
            <Border Background="#FFFFF4DE" CornerRadius="4" Padding="7,7" Visibility="{Binding ShowTodayEarnings, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel><TextBlock Text="今日赚了" Style="{StaticResource MetricLabelStyle}" /><TextBlock Text="{Binding TodayEarningsText}" Style="{StaticResource MetricValueStyle}" FontSize="15" Margin="0,2,0,0" TextTrimming="CharacterEllipsis" /></StackPanel>
            </Border>
        </UniformGrid>
    </Grid>
</UserControl>
