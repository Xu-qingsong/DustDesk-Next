<UserControl x:Class="DustDesk.Next.Widgets.ProjectWidgetView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"><ListBox ItemsSource="{Binding Projects}" BorderThickness="0" Background="Transparent"><ListBox.ItemTemplate><DataTemplate><StackPanel Margin="3,6"><TextBlock Text="{Binding Name}" FontWeight="SemiBold" /><ItemsControl ItemsSource="{Binding Phases}"><ItemsControl.ItemTemplate><DataTemplate><Grid Margin="8,4"><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="80" /></Grid.ColumnDefinitions><TextBlock Text="{Binding Title}" /><ProgressBar Grid.Column="1" Value="{Binding CalculatedProgress, Mode=OneWay}" Height="6" VerticalAlignment="Center" /></Grid></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></StackPanel></DataTemplate></ListBox.ItemTemplate></ListBox></UserControl>
