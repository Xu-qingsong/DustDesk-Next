<UserControl x:Class="DustDesk.Next.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="34,30,34,34" MaxWidth="1180" HorizontalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="{Binding Greeting}" Style="{StaticResource PageTitleStyle}" />
                    <TextBlock Text="先完成重要的事，其余内容自然会归位。" Style="{StaticResource CaptionStyle}" Margin="0,5,0,0" FontSize="14" />
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding DateText}" FontWeight="SemiBold" FontSize="16" HorizontalAlignment="Right" />
                    <TextBlock Text="今日工作台" Style="{StaticResource CaptionStyle}" HorizontalAlignment="Right" Margin="0,4,0,0" />
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Margin="0,26,0,18" Padding="20,16" Background="{StaticResource SummarySurfaceBrush}" CornerRadius="6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="1" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="1" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="今天的节奏" Foreground="White" FontWeight="SemiBold" FontSize="16" />
                        <TextBlock Text="清空未完成事项，比填满日程更重要。" Foreground="{StaticResource SummaryMutedBrush}" FontSize="12" Margin="0,4,0,0" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Width="92" Margin="24,0" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding Workspace.OpenCount}" Foreground="White" FontFamily="Consolas" FontSize="22" HorizontalAlignment="Center" />
                        <TextBlock Text="待处理" Foreground="{StaticResource SummaryMutedBrush}" FontSize="11" HorizontalAlignment="Center" />
                    </StackPanel>
                    <Border Grid.Column="2" Background="{StaticResource SummaryDividerBrush}" Margin="0,2" />
                    <StackPanel Grid.Column="3" Width="92" Margin="24,0" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding Workspace.CompletedCount}" Foreground="{StaticResource SummarySuccessBrush}" FontFamily="Consolas" FontSize="22" HorizontalAlignment="Center" />
                        <TextBlock Text="已完成" Foreground="{StaticResource SummaryMutedBrush}" FontSize="11" HorizontalAlignment="Center" />
                    </StackPanel>
                    <Border Grid.Column="4" Background="{StaticResource SummaryDividerBrush}" Margin="0,2" />
                    <StackPanel Grid.Column="5" Width="92" Margin="24,0,0,0" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding Workspace.TotalCount}" Foreground="{StaticResource SummaryInfoBrush}" FontFamily="Consolas" FontSize="22" HorizontalAlignment="Center" />
                        <TextBlock Text="总计" Foreground="{StaticResource SummaryMutedBrush}" FontSize="11" HorizontalAlignment="Center" />
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.35*" />
                    <ColumnDefinition Width="18" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <Border Style="{StaticResource MainGlassPanelStyle}" MinHeight="380">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="今天要做" Style="{StaticResource SectionTitleStyle}" />
                                <TextBlock Text="勾选完成，变更会自动保存。" Style="{StaticResource CaptionStyle}" Margin="0,4,0,0" />
                            </StackPanel>
                            <Border Grid.Column="1" Background="{StaticResource PrimarySoftBrush}" CornerRadius="4" Padding="9,5">
                                <TextBlock Text="{Binding TodayOpenCount, StringFormat={}{0} 项待处理}" Foreground="{StaticResource PrimaryBrush}" FontSize="12" FontWeight="SemiBold" />
                            </Border>
                        </Grid>

                        <Grid Grid.Row="1" Margin="0,14,0,0"><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBox Text="{Binding NewTodoTitle, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MainGlassInputStyle}" ToolTip="新任务标题" /><Button Grid.Column="1" Content="&#xE710;" FontFamily="Segoe Fluent Icons" Command="{Binding AddTodoCommand}" Style="{StaticResource IconButtonStyle}" ToolTip="添加任务" Margin="6,0,0,0" /></Grid>
                        <ListBox Grid.Row="2" Margin="0,12,0,0" ItemsSource="{Binding TodayTodos}" BorderThickness="0" Background="Transparent" HorizontalContentAlignment="Stretch" ScrollViewer.VerticalScrollBarVisibility="Auto">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="0" />
                                    <Setter Property="Margin" Value="0,0,0,8" />
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="Focusable" Value="False" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <ContentPresenter />
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource CanvasBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="12,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <CheckBox IsChecked="{Binding IsCompleted}" VerticalAlignment="Center" Margin="0,0,11,0" />
                                            <TextBlock Grid.Column="1" Text="{Binding Title}" FontSize="14" VerticalAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                                <Setter Property="TextDecorations" Value="Strikethrough" />
                                                                <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <TextBlock Grid.Column="2" Text="{Binding Tag}" Foreground="{Binding TagColorArgb, Converter={StaticResource ArgbToBrushConverter}}" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource MainGlassPanelStyle}" Background="{StaticResource NoteBrush}" BorderBrush="#E6CF78" MinHeight="380">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <StackPanel>
                            <TextBlock Text="快速记录" Style="{StaticResource SectionTitleStyle}" />
                            <TextBlock Text="不用整理，先把想法留下。" Foreground="#796A35" FontSize="13" Margin="0,4,0,0" />
                        </StackPanel>
                        <TextBox Grid.Row="1"
                                 Text="{Binding Workspace.QuickNote, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,16,0,12"
                                 Padding="0"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 FontSize="15"
                                 Foreground="#3B3524"
                                 AutomationProperties.Name="快速记录内容" />
                        <StackPanel Grid.Row="2" Orientation="Horizontal">
                            <TextBlock FontFamily="Segoe Fluent Icons" Text="&#xE73E;" Foreground="#796A35" Margin="0,0,7,0" />
                            <TextBlock Text="输入后自动保存到本机" Foreground="#796A35" FontSize="12" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
